import { HeaderNew, UserMenuNew, BackButton } from "./components/newNavigation";

export default function MainPage() {
    return (
        <HeaderNew>
            <BackButton/>
            <UserMenuNew/>
            Content
        </HeaderNew>
    );
}
