import React from "react";
import { Box } from "@mui/material";

export default function Shorthand() {
    return (
        <Box
            sx={{
                key
            }}
        >
            Styled with sx prop
        </Box>
    );
}
